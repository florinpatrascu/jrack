Two simple Servlet apps provided as a demo:

1) Echo
   "echo" style Rack web application that can be accessed like this:

        curl http://localhost:8080/echo -d 'hi'

   POST some data to it and it simply returns back the data you posted.


2) Hello World
   point your browser to:

        http://localhost:8080/hello
